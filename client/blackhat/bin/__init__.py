from . import touch, ifconfig, rm, printenv, mv, installable, rmdir, ping, ssh, nano, uname, sha512sum, exit, sudo, \
    echo, chmod, uptime, passwd, sha1sum, cat, hostname, sha256sum, curl, pwd, base32, su, chown, save, adduser, \
    sha224sum, base64, cd, who, export, cp, date, commands, id, tail, dig, clear, ls, wc, env, md5sum, man, sha384sum, \
    whoami, reboot, head, apt, unset, mkdir, users, load
