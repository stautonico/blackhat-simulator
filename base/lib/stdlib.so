function getenv(key) {
    // We don't have a pointer to the environment, so we'll just fake it (like my girlfriend :( )
    return _internal_get_env(key);

}

function setenv(key, value) {
    return _internal_set_env(key, value);
}