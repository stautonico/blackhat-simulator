def main(args):
    if len(args) < 2:
        print(f"usage: write <path> <data>")
        return 1

    result = _syscall(0x1, args[0], " ".join(args[1:]))

    # TODO: Support printing non-string types?
    print(f"Wrote {result} bytes")

    return 0