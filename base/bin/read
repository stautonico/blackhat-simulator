require("errno")
require("stdio")

def main(args):
    if len(args) != 1:
        print(f"usage: read <path>")

    result = _syscall(0x0, args[0])

    if result is None:
        perror("Unable to read file")
    else:
        print(result)

    return 0